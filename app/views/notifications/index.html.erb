<div class=container>
  <div class="wrapper notification_style col-md-6 col-md-offset-3 col-sm-10 col-sm-offset-1">
    <h2>お知らせ</h2>
    <div class=flash_notice><%= notice %></div>
    <ul>
      <% @notifications.each do |notification| %>
      <hr>
        <li>
          <span><%= read_status(notification.read) %></span>
          <p>
            <%= notification.comment.user.try(:name) %>さんが
            あなたのブログ（<%= link_to "#{notification.comment.blog.title}", blog_path(notification.comment.blog, notification_id: notification.id) %>）にコメントしました。
          </p>
          <p><%= posted_time(notification.created_at) %></p>
        </li>
      <% end %>
    </ul>
  <div class=home><%= link_to "ホーム", root_path %></div>
  </div>
</div>
